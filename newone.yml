---
- hosts: newone
  gather_facts: false
  become: yes

  tasks:
    - name: update apt cache
      command: apt update

    - name: install qemu guest agent
      apt:
        name: qemu-guest-agent
        state: present

    - name: upgrade all packages
      command: apt upgrade -y

    - name: set-keyma, fuer Tastaturlayout deutsch
      command: localectl set-keymap de
    
    - name: reboot host
      reboot:
